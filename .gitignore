# Cargo
debug/
target/

# User
/*.txt
/*.exe
/*.tgz
.DS_Store

# Dev
/old
/.temp
/logs
/results

# Benchmark
/benchmark/**/*.txt
/benchmark/**/*.md
/benchmark/**/*.tsv
/benchmark/**/*.log
!/benchmark/README.md
# Chunk-size study: commit the results + README (exception to the broad ignore
# above), but NOT the input fixtures — those are large and regenerated
# deterministically by scripts/sweep-chunk-explorer.sh from fixed seeds.
!/benchmark/chunk-size-study/README.md
!/benchmark/chunk-size-study/results/**
